name: Haskell CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    strategy:
      matrix:
        platform: [ubuntu-latest, windows-latest, macos-latest]
        #        ghc: ['7.4.2', '7.6.3', '7.8.4', '7.10.3', '8.0.2', 
        ghc: [ '8.4.4', '8.6.5'
             , '8.8.4', '8.10.3']
        exclude:
          - platform: macos-latest
            ghc: '8.4.4'
          - platform: macos-latest
            ghc: '8.6.5'
          - platform: macos-latest
            ghc: '8.10.3'
        include:
          - ghc: '8.4.4'
            cabal: '3.2.0.0'
            build: 'build --enable-tests --enable-benchmarks'
            test: 'test --show-details=streaming --log=/dev/stdout'
          - ghc: '8.6.5'
            cabal: '3.2.0.0'
            build: 'build --enable-tests --enable-benchmarks'
            test: 'test --show-details=streaming --log=/dev/stdout'
          - ghc: '8.8.4'
            cabal: '3.0.0.0'
            build: 'build --enable-tests --enable-benchmarks all'
            test: 'test all'
          - ghc: '8.10.3'
            cabal: '3.2.0.0'
            build: 'build --enable-tests --enable-benchmarks all'
            test: 'test all'
    runs-on: ${{ matrix.platform }}

    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-haskell@v1
      with:
        ghc-version: ${{ matrix.ghc }}
        cabal-version: ${{ matrix.cabal }}

    - name: Cache
      uses: actions/cache@v1
      env:
        cache-name: cache-cabal
      with:
        path: ~/.cabal
        key: ${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('**/*.cabal') }}-${{ hashFiles('**/cabal.project') }}
        restore-keys: |
          ${{ runner.os }}-build-${{ env.cache-name }}-
          ${{ runner.os }}-build-
          ${{ runner.os }}-

    - name: Install dependencies
      run: |
        cabal update
        cabal build --only-dependencies --enable-tests --enable-benchmarks
    - name: Build
      run: cabal ${{ matrix.build }}
    - name: Run tests
      run: cabal ${{ matrix.test }}
